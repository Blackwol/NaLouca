               <div class="thumbnail">
                    <%= image_tag @food.picture.url(:large), class:"img-responsive"%>
                    <div class="caption-full">
                        <h4 class="pull-right"><%=@food.price%></h4>
                        <h4><a href="#"><%= @food.title%></a>
                        </h4>
                        <p><%=@food.description%></p>
                    </div>                    
                </div>

                <div class="well">

                    
                      <div class="text-right">
                        <!-- Button trigger modal -->
                        <button type="button" class="btn btn-success" data-toggle="modal" data-target="#commentModal">
                          Deixe um comentário
                        </button>
                      </div>

                    <%= render partial: 'site/food_detail/comments' %>

                </div>

<%= render partial: 'site/food_detail/modal' %>